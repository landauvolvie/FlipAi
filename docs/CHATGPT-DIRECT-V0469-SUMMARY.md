# v0.46.9 summary

FlipAi now parses the installed Electron `app.asar` archive, filters generic bundled tooling, attributes backend markers to exact regular-app files, and extracts safe IPC/preload bridge channel names. The feature remains diagnostic-only until a callable regular ChatGPT-owned background interface is proven.
