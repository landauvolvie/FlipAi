name: temporary-v0469-integration
on:
  push:
    branches:
      - feature/chatgpt-asar-deep-scan-v0469
    paths:
      - .github/workflows/_tmp-patch-v0469c.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/chatgpt-asar-deep-scan-v0469
          fetch-depth: 0
      - name: Patch files
        run: python scripts/patch_v0469.py
      - name: Commit integration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Integrate ChatGPT app.asar deep diagnostic v0.46.9"
          git push origin HEAD:feature/chatgpt-asar-deep-scan-v0469
