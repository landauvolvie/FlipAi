name: temp-provider-isolation-patch

on:
  push:
    branches:
      - fix/browser-agent-reliability

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo ok
