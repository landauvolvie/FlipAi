name: release-attachment-bridge

on:
  push:
    branches: [main]
    paths:
      - VERSION

permissions:
  contents: write

jobs:
  release-windows-x64:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.23.x'

      - name: Read release version
        id: version
        shell: pwsh
        run: |
          $version = (Get-Content VERSION -Raw).Trim()
          if ($version -ne '0.27.0') { throw "This one-time release workflow only permits 0.27.0, got $version" }
          "version=$version" >> $env:GITHUB_OUTPUT
          "tag=v$version" >> $env:GITHUB_OUTPUT

      - name: Generate branded FlipAi icon
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force dist | Out-Null
          .\scripts\Generate-FlipAiIcon.ps1 -OutputPath .\dist\FlipAi.ico

      - name: Test
        run: go test ./...

      - name: Vet
        run: go vet ./...

      - name: Race test
        run: go test -race ./...

      - name: Build Windows x64 app
        shell: pwsh
        run: |
          go build -trimpath -ldflags "-H=windowsgui -s -w" -o dist\FlipAi.exe .
          if (-not (Test-Path .\dist\FlipAi.exe)) { throw 'FlipAi.exe was not built' }

      - name: Microsoft Defender scan app when available
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          if (-not (Get-Command Get-MpComputerStatus -ErrorAction SilentlyContinue) -or -not (Get-Command Start-MpScan -ErrorAction SilentlyContinue)) { exit 0 }
          $status = Get-MpComputerStatus -ErrorAction SilentlyContinue
          if ($null -eq $status -or -not $status.AntivirusEnabled) { exit 0 }
          $exe = (Resolve-Path '.\dist\FlipAi.exe').Path
          Start-MpScan -ScanType CustomScan -ScanPath $exe
          if (Get-Command Get-MpThreatDetection -ErrorAction SilentlyContinue) {
            $hits = @(Get-MpThreatDetection -ErrorAction SilentlyContinue | Where-Object {
              $_.Resources -and (($_.Resources -join "`n") -match [regex]::Escape($exe))
            })
            if ($hits.Count -gt 0) { throw "Microsoft Defender reported $($hits.Count) detection(s) for FlipAi.exe" }
          }

      - name: Install Inno Setup compiler if needed
        shell: pwsh
        run: |
          $candidates = @(
            "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe",
            "$env:ProgramFiles\Inno Setup 6\ISCC.exe"
          )
          $iscc = $candidates | Where-Object { Test-Path $_ } | Select-Object -First 1
          if (-not $iscc) {
            choco install innosetup --yes --no-progress
            $iscc = $candidates | Where-Object { Test-Path $_ } | Select-Object -First 1
          }
          if (-not $iscc) { throw 'Inno Setup compiler was not found' }
          "ISCC=$iscc" >> $env:GITHUB_ENV

      - name: Build Windows Setup EXE
        shell: pwsh
        run: |
          $version = '${{ steps.version.outputs.version }}'
          Remove-Item .\installer-output -Recurse -Force -ErrorAction SilentlyContinue
          & $env:ISCC "/DMyVersion=$version" "/DSourceDir=$((Resolve-Path .\dist).Path)" .\installer\FlipAi.iss
          if ($LASTEXITCODE -ne 0) { throw "ISCC failed with exit code $LASTEXITCODE" }
          $setup = ".\installer-output\FlipAi-Setup-v$version.exe"
          if (-not (Test-Path $setup)) { throw "Setup EXE not found: $setup" }

      - name: Silent installer smoke test
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $version = '${{ steps.version.outputs.version }}'
          $setup = (Resolve-Path ".\installer-output\FlipAi-Setup-v$version.exe").Path
          $installDir = Join-Path $env:RUNNER_TEMP 'FlipAi-v0270-Installed'
          Remove-Item $installDir -Recurse -Force -ErrorAction SilentlyContinue
          $p = Start-Process -FilePath $setup -ArgumentList @('/VERYSILENT','/SUPPRESSMSGBOXES','/NORESTART',"/DIR=`"$installDir`"") -PassThru -Wait
          if ($p.ExitCode -ne 0) { throw "installer exited with $($p.ExitCode)" }
          $app = Join-Path $installDir 'FlipAi.exe'
          $uninstaller = Join-Path $installDir 'unins000.exe'
          if (-not (Test-Path $app)) { throw 'installed FlipAi.exe is missing' }
          if (-not (Test-Path $uninstaller)) { throw 'uninstaller is missing' }
          $u = Start-Process -FilePath $uninstaller -ArgumentList @('/VERYSILENT','/SUPPRESSMSGBOXES','/NORESTART') -PassThru -Wait
          if ($u.ExitCode -ne 0) { throw "uninstaller exited with $($u.ExitCode)" }
          Start-Sleep -Seconds 1
          if (Test-Path $app) { throw 'FlipAi.exe remained after uninstall' }

      - name: Microsoft Defender scan installer when available
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          if (-not (Get-Command Get-MpComputerStatus -ErrorAction SilentlyContinue) -or -not (Get-Command Start-MpScan -ErrorAction SilentlyContinue)) { exit 0 }
          $status = Get-MpComputerStatus -ErrorAction SilentlyContinue
          if ($null -eq $status -or -not $status.AntivirusEnabled) { exit 0 }
          $version = '${{ steps.version.outputs.version }}'
          $setup = (Resolve-Path ".\installer-output\FlipAi-Setup-v$version.exe").Path
          Start-MpScan -ScanType CustomScan -ScanPath $setup
          if (Get-Command Get-MpThreatDetection -ErrorAction SilentlyContinue) {
            $hits = @(Get-MpThreatDetection -ErrorAction SilentlyContinue | Where-Object {
              $_.Resources -and (($_.Resources -join "`n") -match [regex]::Escape($setup))
            })
            if ($hits.Count -gt 0) { throw "Microsoft Defender reported $($hits.Count) detection(s) for FlipAi Setup" }
          }

      - name: Prepare release files
        id: package
        shell: pwsh
        run: |
          $version = '${{ steps.version.outputs.version }}'
          $setup = ".\installer-output\FlipAi-Setup-v$version.exe"
          $hash = (Get-FileHash $setup -Algorithm SHA256).Hash.ToLower()
          "$hash  FlipAi-Setup-v$version.exe" | Out-File -Encoding ascii .\installer-output\SHA256SUMS.txt
          "setup=$setup" >> $env:GITHUB_OUTPUT

      - name: Upload tested installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: FlipAi-Windows-Setup-v0.27.0
          path: |
            installer-output/FlipAi-Setup-v0.27.0.exe
            installer-output/SHA256SUMS.txt

      - name: Publish GitHub Release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $tag = '${{ steps.version.outputs.tag }}'
          $setup = '${{ steps.package.outputs.setup }}'
          $sums = '.\installer-output\SHA256SUMS.txt'
          $notes = @'
          FlipAi can now relay inbound Google Voice MMS attachments to the selected desktop agent.

          - Images are passed as the actual image file.
          - Google Voice voice notes such as audio/mp4 are passed as the actual audio file.
          - Voice notes are automatically instructed to be treated as the user's command: understand it, execute it, and return the result rather than only transcribing it.
          - No local AI model, Whisper service, Drive upload, or outbound attachment delivery was added.
          - Existing text SMS behavior remains unchanged.

          Download **FlipAi-Setup-v0.27.0.exe**.
          '@
          gh release view $tag *> $null
          if ($LASTEXITCODE -eq 0) { throw "Release $tag already exists; refusing to overwrite it" }
          gh release create $tag $setup $sums --target $env:GITHUB_SHA --title "FlipAi $tag" --notes $notes
          if ($LASTEXITCODE -ne 0) { throw "Could not create release $tag" }
